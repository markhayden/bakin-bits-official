# TOOLS.md - Local Notes

Per-install specifics only — skills define how tools work; this file holds
what's unique to your setup (API keys by env-var name, never secret values;
device names; preferred defaults).

<!-- Budget: ≤120 words (CI-enforced). -->
